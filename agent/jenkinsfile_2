pipeline {
    agent any 

    stages {
        stage('build') {
            steps {
                echo "parentjob"
            }
        }

        stage('triggerChildJob') {
            steps {
                build job: "trigger", wait:true
            }
        }
    }

}